//I can config the tests to run auto on load, but it was not in the requirements do I set it manually


run the command :
 - docker build -t product-backend-api .
 - docker build -t product-ui .
 - docker-compose up --build

to run both projects angular ui, spring boot gradle, and mysql image.

spring boot gradle project:
run the test ProductControllerTest from the class

angular ui:
run the command ng test
